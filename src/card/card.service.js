import { Card } from './card.model.js';
import { HttpError } from '../helpers/HttpError.js';

const getAllCards = () => {
  // get all cards
};

const getOneCard = () => {
  // get one card
};

const createOneCard = () => {
  // create one card
};

const deleteOneCard = () => {
  // delete one card
};

const patchOneCard = () => {
  // patch one card
};

export default {
  getAllCards,
  getOneCard,
  createOneCard,
  deleteOneCard,
  patchOneCard,
};
